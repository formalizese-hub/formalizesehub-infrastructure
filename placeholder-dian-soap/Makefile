# Placeholder build — el jar real se despliega desde el repo formalizesehub-dian-soap
# via GitHub Actions con aws lambda update-function-code
build-DianSoapFunction:
	@echo "Skipping build — deployed externally"
	@mkdir -p $(ARTIFACTS_DIR)
	@echo "placeholder" > $(ARTIFACTS_DIR)/placeholder.txt
